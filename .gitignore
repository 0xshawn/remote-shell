# Added by code-review-graph
.code-review-graph/

# Go build output
/server/remote-shell

# Frontend staged into the server package for embedding (release build only).
# Keep the placeholder that makes //go:embed compile.
/server/web/*
!/server/web/.gitkeep

# Local environment / secrets
.env

# nginx TLS certs (keep the dir, ignore the keys)
/deploy/nginx/certs/*.pem
